# GCP
GCP_PROJECT_ID=slcc-buzz-agent-dev

# 검수 임계값
ROW_COUNT_MIN=2000
ROW_COUNT_MAX=2400

# Outlook 알림 (본인 계정 메일 주소)
OUTLOOK_RECIPIENT=your_email@atkearney.com

# 경로 (기본값 그대로 두면 됨)
SQL_QUERY_PATH=sql/unpk_query.sql
OUTPUT_DIR=output
LOG_DIR=logs
